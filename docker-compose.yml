services:
  # Next.js Frontend Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_YOMU_API_BASE_URL: ${NEXT_PUBLIC_YOMU_API_BASE_URL:-http://host.docker.internal:8081}
        NEXT_PUBLIC_RUST_ENGINE_BASE_URL: ${NEXT_PUBLIC_RUST_ENGINE_BASE_URL:-http://host.docker.internal:8080}
        NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID:-}
    container_name: yomu-frontend
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      NEXT_PUBLIC_YOMU_API_BASE_URL: ${NEXT_PUBLIC_YOMU_API_BASE_URL:-http://host.docker.internal:8081}
      NEXT_PUBLIC_RUST_ENGINE_BASE_URL: ${NEXT_PUBLIC_RUST_ENGINE_BASE_URL:-http://host.docker.internal:8080}
      NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${NEXT_PUBLIC_GOOGLE_CLIENT_ID:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

networks:
  default:
    name: yomu-frontend-network
